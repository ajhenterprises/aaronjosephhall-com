---
// Rendered only on entries seeded from the migration inventory that are
// still waiting on verbatim original content. Never delete this by writing
// placeholder prose into the body — replace the body with the real text and
// remove the `contentPending` flag in the entry's frontmatter instead.
---

<div class="content-pending-notice not-prose" role="note">
  <strong>Content pending.</strong> This entry was seeded from the verified migration
  inventory (title, date, category, URL) but is still waiting on the original body
  copy from aaronjosephhall.com. Nothing below is final.
</div>

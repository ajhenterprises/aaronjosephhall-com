---
// Small notice shown at the top of every page via BaseLayout, except
// /work-with-me itself (already leads with this) and articles (which get
// their own larger top/bottom CTA instead). New pages get it automatically
// unless they opt out with hideWorkNotice on BaseLayout.
---

<div class="border-b border-ink/10 bg-parchment-dim/50">
  <p class="mx-auto max-w-6xl px-6 py-2 text-center text-xs text-ink-soft">
    Also available for website development &amp; hosting and content + copywriting —
    <a href="/work-with-me" class="font-medium text-clay hover:underline">see my services &rarr;</a>
  </p>
</div>
